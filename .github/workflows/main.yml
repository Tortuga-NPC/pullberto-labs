name: ping

on:
  push:
    branches:
      - main

jobs:
  say-hi:
    runs-on: [self-hosted, ephemeral]
    steps:
      - run: echo "🐧 Hello desde el runner épico Pullbertito"
      - run: echo "test"
